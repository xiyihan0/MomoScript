# Git internals (avoid committing nested repos)
.git/

# Python bytecode / caches
__pycache__/
*.py[cod]
*$py.class

# Virtual environments
.venv/
venv/
ENV/

# Test / type-check / lint caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/

# IDEs
.idea/
.vscode/

# OS junk
.DS_Store
Thumbs.db

# Local env / secrets
.env
.env.*

# Project caches / temp outputs
.cache/
.tmp/
*.log

# Large downloaded assets (regenerate via scripts)
# images/
mmt_render/images/

# If you want to version only tags metadata (but not images), replace the line above
# with the following block:
# mmt_render/images/**
images/**
!images/students/
!images/students/**/
!images/students/**/tags.json

# Avatar images are large; keep mapping JSONs
avatar/*
!avatar/name_to_id.json
!avatar/download_errors.json

# Packs: ignore large assets, keep metadata/json
mmt_packs/**
!mmt_packs/README.md
!mmt_packs/**/manifest.json
!mmt_packs/**/char_id.json
!mmt_packs/**/asset_mapping.json
!mmt_packs/**/images/**/tags.json

# Pack v2 migration workspace (same policy as mmt_packs)
pack-v2/**
!pack-v2/**/manifest.json
!pack-v2/**/char_id.json
!pack-v2/**/asset_mapping.json
!pack-v2/**/images/**/tags.json

# Copy under mmt_render is cache only
mmt_render/avatar/

# Typst / exports
*.pdf

# mmt_render generated artifacts
mmt_render/*.pipe.json
mmt_render/*.pipe.resolved.json
mmt_render/*.pipe.pdf
mmt_render/*.resolved.json
mmt_render/*.report.json
mmt_render/*.render*.json

# MomoScript Test Examples
mmt_render/*.txt
